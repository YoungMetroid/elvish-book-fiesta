spring:
  application:
    name: library
  datasource:
    url: jdbc:sqlite:/home/fomni/GitRepos/elvish-book-fiesta/db/library.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true